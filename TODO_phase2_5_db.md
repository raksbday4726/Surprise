# Phase 2.5 — Flask ↔ MySQL connection (work log)

- [x] Created `.env` with MySQL env vars and UPLOAD_FOLDER
- [x] Added `api/health.py` GET /api/health
- [x] Registered health blueprint in `app.py`

